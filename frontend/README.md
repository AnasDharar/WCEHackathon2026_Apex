# Manah Arogya Frontend

This frontend is part of the `acm2k26` monorepo. Please see the root README for full setup, features, and backend instructions.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Env:

```env
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1
```
